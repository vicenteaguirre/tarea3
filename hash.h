#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "lecturacsv.h"

typedef char ** arreglo;

arreglo tabla;

void busqueda_hash(char letra);
void buscar_hash(Lista * lista);
void hash();